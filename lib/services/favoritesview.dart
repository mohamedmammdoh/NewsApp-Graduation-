import 'package:flutter/material.dart';

class FavoutitesView extends StatelessWidget {
  const FavoutitesView({super.key});
  static String routenmae = 'favoutiteview';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Favoutites View'),
      ),
    );
  }
}
